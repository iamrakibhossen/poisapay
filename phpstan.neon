includes:
    - vendor/larastan/larastan/extension.neon
    - phpstan-baseline.neon

parameters:
    level: 6
    paths:
        - app
    # Legacy debt is captured in phpstan-baseline.neon so CI stays green while
    # preventing NEW violations. Regenerate after large refactors:
    #   vendor/bin/phpstan analyse --generate-baseline
    # NOTE: the P2P module is now fully analysed (P1.7, 2026-07-26) — its models
    # were annotated (@property + relation generics) and its residual errors fixed,
    # so it is no longer excluded here.
    reportUnmatchedIgnoredErrors: false
    checkModelProperties: true
